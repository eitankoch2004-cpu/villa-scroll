# CLAUDE.md — Glowora

## מה זה הפרויקט
אתר מכירה של מנורות דקורטיביות.

הקונספט: הגולש "נכנס" לווילה, גולל בין חדרים, רואה מנורות ממוקמות
בתוך הסצנות, לוחץ על מנורה ומגיע לעמוד מוצר.

**סטטוס: בבנייה.** אין עדיין מוצרים אמיתיים, ספק, או מכירה אחת.
המנורות והמחירים שבקוד הם מקומות שמורים (placeholders).

## סדר עדיפויות נוכחי
קודם **בונים ומוציאים תוצאות יפות החוצה**.
נושאים עסקיים, חוקיים ומיסויים — בשלב מאוחר יותר, במכוון.

## החלטות שנסגרו
| נושא | ההחלטה |
|---|---|
| שם המותג | **Glowora** |
| שפה | **עברית, RTL** |
| מודל קנייה | הלקוח יוצר קשר לפני קנייה. **אין צ'קאאוט** |
| ריפוזיטורי | אחד. villa-scroll הוא הבסיס |

## החלטות פתוחות
- מודל עסקי: קונסיירז' / מלאי מקומי / דרופשיפינג קלאסי
- אילו מנורות בדיוק, ומאיזה ספק
- מאיפה מגיעות תמונות המוצר (אין מנורות פיזיות ביד)
- דומיין משלו במקום `github.io`

---

## הסטאק — מה באמת יש כאן
HTML + CSS + JavaScript גולמי.

⚠️ **אפס ספריות חיצוניות. אין GSAP. אין ScrollTrigger. אין jQuery.**

רצף הגלילה ממומש ב-`<canvas>` עם מאזין `scroll` ידני, החלקה
(interpolation) ונפילה לפריים הקרוב ביותר שנטען. זה עובד טוב.
**אל תוסיף ספרייה בלי לשאול קודם.**

אחסון: **GitHub Pages** — אתר סטטי. אין שרת, אין בסיס נתונים,
אין אפשרות לסליקת אשראי אמיתית. מודל "צור קשר" עוקף את זה.

---

## מבנה הקבצים
```
villa-scroll/
├── index.html            ← מסך הכניסה + רצף הגלילה
├── showroom.html         ← חדר התצוגה, שלוש המנורות
├── data.js               ← ⭐ כל התוכן. חדרים, מנורות, מחירים, פרטי קשר
├── frames-lobby/         ← הכניסה, דסקטופ (0001–0120.webp)
├── frames-lobby-m/       ← הכניסה, מובייל
├── frames-approach*/     ← הסצנה הישנה. count:0, שמורה לעתיד
├── legacy/               ← ארכיון הדפים הישנים. לא חלק מהאתר
├── tools/preview.ps1     ← שרת תצוגה מקומית
├── tools/frames.ps1      ← סרטון → פריימים (דורש ffmpeg)
└── .claude/              ← הגדרות ופקודות קיצור
```

## מסרטון לסצנה
ffmpeg מותקן (winget: `Gyan.FFmpeg`). ליצירת חדר חדש מסרטון:
```
.\tools\frames.ps1 -Video "C:\...\clip.mp4" -Name kitchen -Count 120 -Grade dusk -Quality 66
```
`-Grade` = `none` / `dusk` / `night`. "יום ללילה" עובד **רק** על צילום בלי
שמיים שרופים בפריים — אחרת הזיוף נראה.
`-Quality 66` הוא האיזון שנבחר: נקי לעין, בלי משקל מיותר.

## ⭐ המוסכמה החשובה ביותר
בתוך [index.html](index.html) יש בלוק מסומן `EDIT ONLY THIS BLOCK`.
כל התוכן של האתר יושב שם בשני מערכים:

- **`SEGMENTS`** — חדרי הווילה. כל חדר: תיקיית פריימים, כמות, כותרת, טקסט.
  `count: 0` = החדר עוד לא נבנה, והקוד מדלג עליו אוטומטית.
- **`LAMPS`** — המנורות. מזהה, חדר, שם, מחיר, תיאור.

**להוסיף חדר או מנורה = לערוך את המערכים האלה בלבד.**
הקוד שמתחת לבלוק נבנה כדי לא לגעת בו. אם משימה דורשת לשנות אותו —
זה סימן לעצור ולשאול.

## פריימים — מוסכמות
| | |
|---|---|
| פורמט | `.webp` בלבד (הקבוע `EXT` בקוד) |
| שמות | 4 ספרות: `0001.webp`, `0042.webp` |
| דסקטופ | תיקיית `frames…`, יחס רחב |
| מובייל | תיקיית `frames…-m`, יחס 4:5 (ידידותי לטלפון) |
| מהירות | `PX_PER_FRAME = 9` — פיקסלים של גלילה לכל פריים. גבוה יותר = הליכה איטית יותר |
| דסקטופ | `PPF = PX_PER_FRAME × 2.5` — ראה למטה |

⚠️ **גלגלת עכבר קופצת ~100px בנקישה; אצבע זזה ברצף.** באותו מרחק גלילה,
נקישה אחת מדלגת על ~15 פריימים והתנועה נראית קופצנית בדסקטופ בלבד.
לכן בדסקטופ הסצנה נפרשת על פי-2.5 מרחק (5 פריימים לנקישה במקום 15).
**זו לא בעיית ביצועים — זו רזולוציית קלט. אל תחפש אותה ב-fps.**

יש כרגע **121 פריימים** לסצנה הראשונה ("The approach").

---

## ⚠️ מגבלת הזיכרון — האילוץ המרכזי בפרויקט
פריים מובייל = 880×1100 = **3.7MB בזיכרון**. פריים דסקטופ = 1600×900 = **5.5MB**.
**Safari באייפון הורג את הדף ב-384MB.**

⚠️ דסקטופ **אינו** בטוח אוטומטית: ב-1920×1080 ועם חלון 24 הגענו ל-622MB
והדפדפן נתקע. לכן פריימי הדסקטופ הוקטנו ל-1600×900 והחלון שם **קטן**
מזה שבמובייל. גודל מסך גדול לא אומר שאפשר להיות פזרן.

טעינה של כל הפריימים הייתה מגיעה ל-448MB עם **חדר אחד**. לכן `index.html`
משתמש ב**חלון זיכרון נע**:
- `WINDOW` — כמה פריימים מחזיקים בכל צד של המיקום הנוכחי (20 במובייל, 16 בדסקטופ)
- `SKELETON` — שלד קבוע, כל פריים שנים־עשר, שלא משתחרר לעולם
- `release()` — משחרר פריימים מחוץ לחלון
- `loadSegment()` — טוען את השלד בשני גלים, כדי שהטעינה הראשונה תישאר קלה

**המצב כרגע (301 פריימים):** מובייל ~247MB · דסקטופ ~324MB · טעינה ראשונה ~0.8MB.

⚠️ `WINDOW` נמדד ב**פריימים**, לא במרחק. סצנה צפופה יותר דורשת חלון גדול יותר,
אחרת הוא מכסה פחות תנועה והגלילה קופצת. **כשמשנים את מספר הפריימים —
לחשב מחדש גם את `WINDOW` וגם את הזיכרון.**

🔴 **כלל:** כל שינוי במנוע הפריימים חייב לשמור על התכונה הזו.
אסור לחזור לטעינה של כל הפריימים מראש.

## 🔴 פענוח לפני ציור — הכלל השני של המנוע
`drawImage` על תמונה שנטענה אך **לא פוענחה** מכריח פענוח סינכרוני על
החוט הראשי. פריים 1600×900 = ~15ms. שלושים כאלה יחד = חצי שנייה של קיפאון.

לכן `grab()` קורא ל-`img.decode()` ומסמן `__ready` רק אחריו,
ו-`nearest()` מחזיר **רק** תמונות עם `__ready`.

⚠️ אל תחליף את הבדיקה `__ready` ב-`complete && naturalWidth`. זה נראה
זהה ומחזיר את הקיפאון.

**אבחון:** להוסיף `?debug` לכתובת — מסך עם fps, זיכרון, כמה פוענחו.
`min 3s` הוא המספר שקובע; `min all` כולל את הטעינה הראשונה.

## 🔢 מספרים נגזרים מהחומר, לא מועתקים
מספר הפריימים של סצנה = **מספר הפריימים שיש בסרטון המקור** (`fps × שניות`).
פחות מזה = זורקים תנועה והגלילה קופצת. יותר = משכפלים תמונות ומוסיפים משקל.

`tools/frames.ps1` מחשב את זה לבד כשלא מציינים `-Count`. **להשתמש בברירת המחדל.**
הסצנה הנוכחית: הסרטון הוא 10.03 שניות ב-30fps → **301 פריימים**.

זה חל על כל מספר בפרויקט: לגזור מהמדידה, לא להעתיק ממה שהיה קודם.

## 🐛 תקלות ידועות — לא לשכוח
| # | תקלה | איפה |
|---|---|---|
| 1 | הגלילה **לא חלקה** לדברי איתן. חשוד ראשון: `WINDOW` קטן מדי | index.html |
| 2 | פרטי הקשר ב-`CONTACT` הם **מספרים מזויפים** | data.js |
| 3 | אין תמונות מוצר — `img/lamp-*.jpg` לא קיימות | index.html, showroom.html |
| 4 | המנורות והמחירים הם placeholders, לא מוצרים אמיתיים | data.js |

**תוקן:** showroom.html נבנה · האתר הומר לעברית ו-Glowora · legacy/bedroom.html
· המחירים אוחדו ל-data.js · חלון הזיכרון · מסך הכניסה

---

## עקרונות עבודה בפרויקט
- **מובייל קודם.** רוב הקונים יגיעו מהטלפון. כל שינוי נבדק במסך צר.
- **משקל התמונות קריטי.** האתר כבר ~20MB. כל תמונה חדשה נשקלת.
- האווירה: כהה, קולנועית, סטודיו אדריכלות. וילה מודרנית, שעת דמדומים.
- קליפים ותמונות של חדרים — ביחס ידידותי למובייל, לא 16:9.

## הטקס — איך עוברים על כל נושא
לכל נושא חדש בפרויקט, בסדר הזה:

| שלב | מי | מה |
|---|---|---|
| 1 | אני | מסביר: מה הנושא, למה חשוב, מה האפשרויות + המלצה |
| 2 | איתן | שואל עד שברור, ואז מחליט |
| 3 | אני | מבצע — צעד אחד |
| 4 | אני | מראה בדיוק מה השתנה |
| 5 | איתן | מאשר → הנושא נסגר ונרשם כאן |

**לא עוברים לנושא הבא עד ששלב 5 קרה.**

## פקודות קיצור
| פקודה | מה היא עושה |
|---|---|
| `/preview` | מפעיל שרת מקומי — לראות את האתר במחשב ובטלפון |
| `/check` | בדיקת בריאות: קישורים שבורים, משקל, טקסטים שנשכחו |
| `/room` | מוסיף חדר חדש לרצף הגלילה, עם בדיקות |
| `/publish` | מכין להעלאה + הוראות GitHub Desktop צעד אחר צעד |
| `/lesson` | שיעור יומי של 15 דקות — נשמר כקובץ Word מעוצב |

## מסמכים נוספים
- [docs/research-2026-07.md](docs/research-2026-07.md) — מחקר שוק: מכס, משלוחים,
  סליקה, אינסטגרם. **לא לחזור על המחקר** אלא אם התיישן.
- [docs/learning/plan.md](docs/learning/plan.md) — תוכנית לימוד, 25 שיעורים
  של 15 דקות. ההתקדמות ב-[docs/learning/progress.md](docs/learning/progress.md),
  קובצי ה-Word ב-[docs/learning/lessons/](docs/learning/lessons/).

## מילון מונחים
- **סצנה / scene** — רצף תמונות של חדר אחד שנגלל.
- **פריים / frame** — תמונה בודדת מתוך הרצף.
- **עמוד מוצר** — דף של מנורה בודדת עם תיאור ומחיר.
- **legacy** — ארכיון. קוד ישן ששמור לעיון, לא חלק מהאתר החי.
